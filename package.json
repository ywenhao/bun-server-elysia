{
  "name": "app",
  "version": "1.0.50",
  "scripts": {
    "start": "./server-bin",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build --compile --minify --target=bun-linux-x64 --outfile server-bin ./src/index.ts"
  },
  "dependencies": {
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
